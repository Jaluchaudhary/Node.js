// using a for loop
let lists = ['Mercury', 'Venus', 'Earth', 'Mars', 'Jupiter', 'Uranus', 'Neptune'];

console.log("Using for of loop:");
for (let list of lists) {
console.log(lists);
}

// Using the traditional for loop with a counter
console.log("Using traditional for loop with counter:");
for (let i = 0; i < lists.length; i++) {
  console.log(lists[i]);
}